2026-04-16: Bootstrap — created `sani.md` and `sani-memory.md` as workspace change log and session context.
2026-04-16: Removed dead `src/assetBuildings.ts`; remapped legacy asset building slots in `src/buildings.ts` to in-repo voxel sets. Updated `src/data.ts` and `src/editor/templates.ts` accordingly.
2026-04-16: Cleaned root temp files (`tmp-*`, stale vite logs); updated `.gitignore`.
2026-04-16: Hardened `useGameSession.ts` — new/continued runs persist immediately before falling back to debounced autosaves. Fixes broken boot/save flow.
2026-04-16: Reworked gold pickup feedback (`streetPickups.ts`, `WorldScene.tsx`, `useFeedbackCleanup.ts`, `session.ts`, `types.ts`) — removed stamina popup, replaced with short-lived `+energy` floater above player.
2026-04-17: Fixed pickup hitching — split world pickup rendering out of terrain remesh path (`WorldScene.tsx`, `VoxelWorldContainer.tsx`, `VoxelEngine.ts`) so collecting pills no longer rebuilds full voxel terrain.
2026-04-17: Added weather system (`weatherSystem.ts`) threaded through types, session, time/curfew loop, movement loop, economy, WorldScene, Header. Weather influences movement, exposure, upkeep, exports, UI warnings. Reworked day/night cycle + world environment for smoother transitions, fog, and precipitation. Extended weather into operations: travel time/cost, mine hazard/yield, and exposure in `navigationActions.ts` and `mineActions.ts`. Hardened smoke regression script.
2026-04-17: Added `docs/game-engine-architecture.md` — engine reference covering boot/session, GameState, scene routing, scheduler, world rendering, entity/environment boundaries, save arch, and structural debt. Updated README.
2026-04-18: Iterated StartScreen world picker (`StartScreen.tsx`, `worldProfiles.ts`) — compressed rows, reduced icon/chevron/text sizing, removed per-world descriptions and explanatory copy, shrunk archive-status save text and Continue button to match tighter density. Repurposed world-3 into Testing Basin sandbox.
2026-04-18: Reworked saves into slot-based archive (`save.ts`, `saveMetadata.json`). Added `SaveArchiveScreen.tsx`. Rewired `useGameSession.ts` + `App.tsx` so Continue opens a save-file picker instead of loading a single hardcoded blob.
2026-04-18: Simplified FTUE UI — removed duplicate Bureau tutorial banners from `WorldScene.tsx`; rebuilt `TutorialOverlay.tsx` into a compact left-edge pill that docks into a small icon for later expansion points.
2026-04-18: Locked live gameplay into a mobile-first handset shell (`App.tsx`, `index.css`) and tightened the existing right-side nav rail (`SideNavPanel.tsx`) so overlays/sidebar stay inside the game screen on larger displays.
2026-04-18: Rebuilt the left-side HUD into icon-driven objective + story ledger controls. `useNotificationCenter.ts` now tracks unread ledger state, `TutorialOverlay.tsx` drops the "Guide" tag and unread clears when opened, `ActionLogPanel.tsx` becomes a left-rail story archive, and `App.tsx` stops using the floating popup notification card.
2026-04-18: Fixed left-rail overlap regression - `ActionLogPanel.tsx` now truly hides its floating toggle when `showToggle={false}`, and `TutorialOverlay.tsx` docks the FTUE pin near the bottom safe area instead of across the office content.
2026-04-19: Rebuilt `scripts/smoke-regression.mjs` for the current UI/save flow. Coverage now validates World 1 boot, `Start Run`, analog movement persistence, mine navigation, archive restore, and a seeded Bureau FTUE filing path through Officer Vane into Form 17-B submission start.
2026-04-19: Reworked `src/components/Header.tsx` by transplanting the old project HUD language into this repo - modular expandable top HUD blocks for status, money, energy, ore, evidence, and core meters, while preserving current state wiring, weather/cycle context, utilities access, and FTUE compact mode.
2026-04-19: Added shared HUD primitives in `src/components/HudFrame.tsx` and pushed that chrome into `MineScene.tsx`, `MineWorldScene.tsx`, and `OfficeExploration.tsx` so mine/exploration surfaces stop falling back to the older white-card/pill styling.
2026-04-19: Locked local dev serving to `127.0.0.1:4173` in `package.json` and `vite.config.ts` with `strictPort` so Vite no longer drifts to a different port than the active browser tab and triggers HMR/WebSocket refusal spam.
2026-04-19: Replaced procedural world-building exports with normalized JSON asset voxels (`src/assetVoxels.ts`, `src/buildings.ts`, `src/voxelData.ts`) so the live world uses the modular asset set from `assets/` instead of hardcoded generator buildings.
2026-04-19: Expanded the runtime world for the larger modular building set (`src/utils/voxelConstants.ts`, `src/data.ts`, `src/editor/derive.ts`, `src/components/cityPlanner/PlannerInspector.tsx`, `src/mineWorldData.ts`). World size is now 360, the city layout is projected onto a wider runtime grid, and editor/mine code now follows the shared bounds.
2026-04-19: Rebuilt the city street graph in `src/data.ts` into a connected grid and moved road-facing lots/foliage cells so buildings stop floating between disconnected roads. Verified with connectivity/overlap probes and `output/world-road-layout-check.png`.
2026-04-19: Made the world editor production-accessible and split `CITY_PLANNER` out of the handset shell (`src/App.tsx`, `src/components/GameSceneRouter.tsx`). Verified on a production preview with fullscreen planner metrics and `output/editor-production-fullscreen-check.png`.
2026-04-20: Reset local `main` to `origin/main` at `ad98edc` (`Expose planner in production`) because this is the last GitHub `main` state before `gamemech` existed. Preserved the previous local head `c9520dd` on backup branch `codex/backup-before-gamemech-reset`.
2026-04-20: Pushed edge skyline placement beyond the playable world in `src/VoxelEngine.ts` so shadow buildings sit on the perimeter instead of overlapping the city, and updated the top-right condition chip in `src/components/WorldScene.tsx` to explicitly label itself as weather.
2026-04-20: Reduced the player house and generic residential house asset scales in `src/assetVoxels.ts` so homes read smaller in the world and their footprints shrink with the visuals instead of only changing appearance.
2026-04-20: Re-zoned the city layout in `src/data.ts` into suburban perimeter housing and a denser inner civic/commercial core with a more coherent road grid; verified no overlaps, valid access tiles, and NPC home/work pathing.
2026-04-20: Added infill assets (`SMALL_COTTAGE_VOXELS`, `CORNER_SHOP_VOXELS`), offset lot placement, promenade buffers, and multi-building suburban blocks in `src/buildings.ts`, `src/data.ts`, and `src/editor/templates.ts`. Added `scripts/validate-city-layout.ts` as a repeatable structural check for overlaps, access, and NPC routes.
2026-04-20: Enabled bounded world-camera rotation in `src/VoxelEngine.ts`, `src/hooks/useCameraControls.ts`, `src/components/VoxelWorldContainer.tsx`, `src/components/WorldScene.tsx`, and `src/components/MineWorldScene.tsx` so the city can be viewed from multiple angles without breaking camera-relative analog movement. Also added verge bands to road tiles in `src/buildings.ts` and a rotate hint to the world HUD.
2026-04-20: Added a one-shot spawn camera intro in `src/VoxelEngine.ts` so the world now opens far out and zooms cleanly into the player before normal camera control takes over.
2026-04-20: Removed the bottom-right rotate hint pill from `src/components/WorldScene.tsx` to keep the world HUD cleaner while leaving camera rotation controls intact.
2026-04-20: Increased passive in-game time pace via shared `src/game/worldPresentation.ts` helper and lifted the rendered world stack by 1 voxel in `src/VoxelEngine.ts` so terrain/overlays stop sitting visually below the baseline. Added regression coverage in `src/game/worldPresentation.test.ts`.
2026-04-20: Lifted generated terrain heights by one step in `src/utils/worldSurface.ts` so sunken grass lots render closer to the road/building plane instead of disappearing at the isometric camera angle.
2026-04-20: Corrected lowered terrain visuals in src/utils/worldSurface.ts so CLIFF tiles keep a grassy top surface instead of rendering grey stone across the entire lowered lot.
2026-04-20: Unified natural terrain palettes in `src/utils/worldSurface.ts` so ground, park, and cliff tiles all render as green earth across every visible layer regardless of elevation.
2026-04-20: Added pedestrian-only NPC routing and off-duty roaming (`src/game/npcNavigation.ts`, `src/EntityManager.ts`, `src/utils/pathfinding.ts`) so NPCs stay on sidewalks/frontage connectors, only cross at intersections, and walk at a capped player-matched pace. Updated planner/layout validation and added `src/game/npcNavigation.test.ts`.
- 2026-04-20: Started world-entry first-person transition work. Added failing camera-plan tests in `src/game/worldEntryCamera.test.ts` to define the target pose before wiring engine/runtime behavior.
2026-04-20: Fixed fresh world-start runtime crash in `src/App.tsx` by removing conditionally introduced hooks from the post-start render path. Updated `scripts/smoke-regression.mjs` to match the direct world-file boot flow, trap runtime React errors, and probe multiple analog directions so the regression suite follows the real UI and current spawn layout.
- 2026-04-21: Tightened the world-start spawn fly-in in `src/game/worldEntryCamera.ts` and `src/VoxelEngine.ts` so intro camera timing/settle are deterministic, controls re-sync cleanly, and recenter/movement no longer inherit stale intro state. Exposed a tiny camera debug snapshot from `src/components/VoxelWorldContainer.tsx` and extended `scripts/smoke-regression.mjs` plus `src/game/worldEntryCamera.test.ts` to catch boot-camera regressions. Also fixed a hook-order crash in `src/App.tsx` by removing late `useCallback` hooks that were declared after the pre-game early return.
- 2026-04-21: Fixed Testing Basin bootstrap in `src/game/worldProfiles.ts` so World 3 now starts with FTUE disabled and core permit/mine progression pre-unlocked for sandbox use. Added `src/game/session.test.ts` to lock World 3 sandbox boot while keeping World 1 on the normal onboarding path.
- 2026-04-21: Softened lowered lot terrain edges in `src/utils/worldSurface.ts` with a dedicated stepped parcel transition surface, updated planner/NPC handling in `src/components/cityPlanner/PlannerCanvas.tsx` and `src/game/npcNavigation.ts`, and added `src/utils/worldSurface.test.ts` so road-to-lot cuts read intentional instead of broken.
- 2026-04-21: Added `docs/godot-migration-plan.md` to lock the decision to move Aureus Below into Godot, identify which gameplay/content systems must survive, and define the phased migration path away from the React runtime.
- 2026-04-21: Scaffolded a real Godot migration workspace under `godot/` with `project.godot`, a main scene, `GameState` and `SceneCoordinator` autoloads, bootstrap JSON for world profiles/effects, and a migration inventory so the port has an executable home instead of staying a planning exercise.
- 2026-04-21: Ported the first real gameplay spine into `godot/` by adding canonical schema docs, JSON seed content for permits/mines/NPCs, deterministic mine-grid generation in `godot/scripts/autoload/game_state.gd`, a GDScript `PermitSystem`, and a bootstrap validator to confirm permit-chain and mine-unlock parity.
- 2026-04-21: Added Godot save metadata and `SaveSystem`, extended `godot/scripts/autoload/game_state.gd` to hydrate saved sessions with FTUE/world-effect defaults and profile-aware base state, and expanded bootstrap validation to cover World 3 sandbox parity plus save/load round-trips.
- 2026-04-21: Added core town building data under `godot/data/content/buildings.json`, plus `WorldSystem`, `ObjectiveSystem`, and `ProgressionSystem` so the Godot runtime now tracks Bureau discovery/entry, Vane contact, and Form 17-B FTUE progression instead of treating those beats as static text.
- 2026-04-21: Added Godot-side `EconomySystem` and `PermitProcessingSystem`, exposed daily tick/export/permit-processing mutations through `godot/scripts/autoload/game_state.gd`, and expanded bootstrap validation to prove exports, daily economy changes, and forced permit approval/rejection outcomes.
- 2026-04-23: Renamed the dropped UI art in `godot/assets/ui/` to stable screen names and updated the mobile MVP scene to use those images as runtime-loaded textured backplates while preserving the live Godot controls and interaction flow.
- 2026-04-23: Reworked `godot/scripts/mvp/mobile_mvp_scene.gd` from generic stacked debug panels into screen-specific overlay placement with clickable town hotspots, art-aligned permit/dialogue/mine controls, and a clean Godot boot against the art-backed MVP scene.
- 2026-04-23: Locked the Godot MVP presentation to a portrait mobile viewport in `godot/project.godot` (`864x1536`, aspect `keep`) so the game letterboxes like a handset experience instead of stretching into a desktop layout.
- 2026-04-25: Sorted the Godot MVP UI asset zips from `godot/assets/unsorted`, promoted selected runtime-ready sprites into `godot/assets/ui/chrome`, `icons`, `mine`, `permits`, and `dialogue` with semantic filenames, moved raw generated concept sheets into `godot/assets/ui/reference`, and added `godot/assets/ui/asset_manifest.md`.
- 2026-04-25: Wired the promoted UI assets into the Godot mobile MVP (`mobile_mvp_scene.gd`) with asset-backed HUD cards, title/footer/action buttons, hub hotspots, dialogue portraits/options, selectable permit cards, mine selectors, and textured mine tiles. Added dialogue JSON for union, inspector, fixer, journalist, and chief, registered those catalogs in `GameState`, and expanded bootstrap validation. Verified with Godot 4.6 headless bootstrap and main-scene boot.
- 2026-04-25: Created an ignored Construct 3 MCP lab under `.codex/construct3-mcp-lab`, cloned `liauw-media/construct3-mcp`, installed/built it, copied its minimal fixture into `test-project`, and added `smoke-readonly.mjs` to prove read-only MCP calls against the fixture before exposing anything over HTTPS.
- 2026-04-25: Added a Supergateway-based Streamable HTTP wrapper for the Construct 3 MCP lab (`start-remote-http.mjs`, `smoke-http.mjs`, `run-smokes.mjs`, lab `package.json`). Verified both stdio and local HTTP MCP calls with `npm test`; endpoint is `http://127.0.0.1:8787/mcp`. Public tunnel is not started because neither `cloudflared` nor `ngrok` is installed on PATH.
- 2026-04-26: Added `.codex/construct3-mcp-lab/import-godot-assets.mjs` and generated a disposable Construct project at `.codex/construct3-mcp-lab/godot-assets-test-project` containing 381 non-`.import` files from `godot/assets` registered under `rootFileFolders.general/godot-assets`. MCP validation reports 0 errors and 0 warnings; packed archive written to `.codex/construct3-mcp-lab/godot-assets-test-project.c3p`.
- 2026-04-30: Added district incident choice cards to the main React game (`types.ts`, `cityIncidents.ts`, `useCityEventLoop.ts`, `CityIncidentOverlay.tsx`, `App.tsx`, smoke/unit tests). Incidents now appear after FTUE as player choices instead of passive auto-resolved city toasts.
- 2026-04-30: Increased street stamina pickup restore from 10 to 20 energy in `src/game/streetPickups.ts` and added `src/game/streetPickups.test.ts` so pickup recovery stays meaningful.
- 2026-04-30: Added `docs/90-improvement-audit.md`, a ranked 90-point technical improvement inventory covering React runtime ownership, deterministic simulation, save/migration hardening, renderer debt, UI flow gaps, tests/tooling, and Godot parity work. No runtime code changed.
- 2026-04-30: Added first audit execution slice: `test:unit`, `validate:city`, and a Windows-safe `check` runner (`scripts/check.mjs`) so typecheck, Node unit tests, city layout validation, and production build can run behind one command.
- 2026-04-30: Hardened save validation by adding `src/game/saveValidation.ts` and tests. `save.ts` now rejects corrupt core save state through a typed validator instead of shallow "likely state" checks while still allowing legacy-compatible fields to hydrate.
