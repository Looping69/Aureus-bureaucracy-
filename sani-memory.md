2026-04-16: Created session anchor files `sani.md` and `sani-memory.md` so the workspace has a local change log and working context.
2026-04-16: Removed dead `src/assetBuildings.ts` JSON adapter and remapped legacy asset building slots in `src/buildings.ts` to maintained in-repo voxel sets. Updated `src/data.ts` and `src/editor/templates.ts` to use the new source so build/lint/runtime flows stop breaking on missing voxel-model JSON files.
2026-04-16: Removed root temp/playtest scrap (`tmp-*`, stale vite logs) and updated `.gitignore` to ignore `tmp-*.cjs` artifacts.
2026-04-16: Hardened `src/hooks/app/useGameSession.ts` so a new/continued run persists immediately before falling back to debounced autosaves. This fixes the broken boot/save flow where early-session state churn could postpone the first save and break regression startup checks.
2026-04-16: Reworked world gold pickup feedback in `src/game/streetPickups.ts`, `src/components/WorldScene.tsx`, `src/hooks/game/useFeedbackCleanup.ts`, `src/game/session.ts`, and `src/types.ts` so stamina pickup popups are removed and replaced with short-lived `+energy` numbers above the player.
2026-04-17: Fixed pickup hitching by splitting world pickup rendering out of the terrain remesh path in `src/components/WorldScene.tsx`, `src/components/VoxelWorldContainer.tsx`, and `src/VoxelEngine.ts` so collecting stamina pills no longer rebuilds the full voxel terrain.
2026-04-17: Added a real weather pipeline (`src/game/weatherSystem.ts`) and threaded it through `src/types.ts`, `src/game/session.ts`, `src/hooks/game/useTimeAndCurfewLoop.ts`, `src/hooks/game/useMovementLoop.ts`, `src/game/economy.ts`, `src/components/WorldScene.tsx`, and `src/components/Header.tsx` so weather now influences movement, exposure, upkeep, exports, and UI warnings. Reworked `src/lighting/dayNightCycle.ts`, `src/lighting/WorldEnvironmentSystem.ts`, `src/components/VoxelWorldContainer.tsx`, and `src/VoxelEngine.ts` so day/night transitions are smoother and the world renderer reacts to weather with better lighting, fog, and precipitation.
2026-04-17: Extended weather from presentation into operations by updating `src/game/navigationActions.ts` and `src/game/actions/mineActions.ts` so travel time, travel cost, mine hazard, mine yield, and exposure all respond to live weather. Hardened `scripts/smoke-regression.mjs` to wait for actual mine scene state instead of a brittle timeout and an operational-only UI button.
